name: Shell Commands

on: push

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: 
          echo "Astrox 100 ZZ"
      - name : display a file    
          cat rackets.txt
      - name: multiline script
        run:
          node -v
          npm -v  